# Journal — Experiment 02 (2026-06-14)

Format: chronological. Note especially the manual route-approval gate.

---

## Pre-run
- Depends on Experiment 01 (cluster `qube-homelab` + echo app up).
- Creds present in `.env`: `TS_LOGIN_SERVER=https://abc.yourcompany.com`,
  `TS_AUTHKEY=<set>`. Route `TS_ROUTES=172.28.210.0/24`.

## Run log
_(to be appended during execution)_

### Step 1 — deploy subnet router  ✅
- `bash tailscale/manage.sh install --cluster-name qube-homelab --authkey … --login-server https://abc.yourcompany.com --routes 172.28.210.0/24`
- ns `tailscale-lb`, secret, Deployment created; deployment condition met.
- Note: `manage.sh` install forces `CLUSTER_CONTEXT=""` → falls back to current
  kubeconfig context (`k3d-qube-homelab`), which worked. (Minor smell, not a blocker.)

### Step 2 — confirm registration (status)  ✅
- Router pod Running on server-0. Logs: `machineAuthorized=true`,
  `routes=[172.28.210.0/24]`, url=`https://abc.yourcompany.com`.
- Tailnet identity: `qube-homelab-ts-router` = `100.64.0.47`; sees full tailnet.
- One transient warning at startup (`failed to resolve abc.yourcompany.com … no DNS
  fallback`) self-resolved on retry; registration succeeded ~10s later.

### Step 3 — MANUAL: approve route in Headscale  ⏳ PENDING OPERATOR
- Advertised route `172.28.210.0/24` from node `qube-homelab-ts-router` must be
  approved/enabled in Headscale before it carries traffic.
- This host (`homelab` = `100.64.0.10`) is itself a tailnet node, but currently has a
  DIRECT route `172.28.210.0/24 dev br-c8b91c8d0283` (from Exp 01). To test the
  TAILNET path from here, that direct route is removed first (else traffic bypasses
  the tailnet). Alternatively verify from any other tailnet machine.
- Notified operator: 2026-06-14 ~14:43
- Operator approved: **PAUSED** — operator cannot reach the Headscale control host.

### Step 3a — can we approve with only the node auth key? NO.
- Verified route NOT approved: our node `100.64.0.47` has `PrimaryRoutes: None`
  (sibling `qube-genai-ts-router` shows `['172.28.240.0/24']` → that's an approved one).
- Headscale API reachable (`/health`=200) but `/api/v1/routes` → **401** with the
  node auth key. The node/pre-auth key only authorizes registration; it has no API
  scope. Route approval needs: control-plane CLI, **or** a Headscale **API key**
  (Bearer), **or** an `autoApprovers` ACL policy. None available from this host.
- Decision: **pause Exp 2**; design the remote-approval prerequisites in **Exp 3**.
  Resume Exp 2 once an API key exists.

### Step 4 — remote verify (dig + curl from tailnet machine)  — pending Exp 2 resume

---

## Problems & solutions
| # | Problem | Root cause | Fix |
|---|---------|-----------|-----|

## Result
_TBD_
