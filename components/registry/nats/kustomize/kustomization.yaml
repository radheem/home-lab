apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: messaging        # stamp metadata.namespace on all rendered resources
helmCharts:
  - name: nats
    repo: https://nats-io.github.io/k8s/helm/charts/
    version: 2.12.4
    releaseName: nats
    namespace: messaging
    valuesFile: values.yaml
