apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - issuers.yaml
# dns01-clusterissuer.yaml.disabled is intentionally NOT included — it is a
# template for the future public-domain ACME path. See docs/cert-manager.md.
