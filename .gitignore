# Secrets and generated state — never commit
.env
kubeconfig-*.yaml
*.key
*.pem
home-lab-ca.crt
# Rendered/temp artifacts
*-resolved.yaml
.render/
.render-*
# Runtime logs
logs/
# Imported external stack — source only, never commit
temp-artifacts/
# Component selection + secrets (live files; commit the .example instead)
components/components.yaml
components/components.secrets.env
